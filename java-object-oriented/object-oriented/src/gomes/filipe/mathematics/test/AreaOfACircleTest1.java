package gomes.filipe.mathematics.test;

import gomes.filipe.mathematics.domain.AreaOfACircle;

public class AreaOfACircleTest1 {
    public static void main(String[] args) {
        AreaOfACircle area = new AreaOfACircle();
        area.calculateAreaOfACircle(10);
        area.calculateAreaOfACircle(3);
        area.calculateAreaOfACircle(7);
        area.calculateAreaOfACircle(4);
        area.calculateAreaOfACircle(5);
    }
}
